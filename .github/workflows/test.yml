name: test

on:
  push:
    branch:
      - master

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        go-version: [ '1.8.x', '1.9.x', '1.20.x, 1.21.x' ]
        platform: [ubuntu-latest, macos-latest, windows-latest]

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: install go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ matrix.go-version }}

      - name: Test
        run: go test ./...  
